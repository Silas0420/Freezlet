var express = require('express');
var webhook = require('./index');

var app = new express();
app.use('/base', webhook({
	hooks: {
		'/route': 'ls',
		'/route/action': 'ls > a.txt'
	}
}));
app.listen('12345');